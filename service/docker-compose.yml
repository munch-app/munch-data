version: '2'

services:
  elastic:
    image: docker.elastic.co/elasticsearch/elasticsearch:6.0.0
    ports:
      - "8050:9200"

  dynamodb:
    image: cnadiminti/dynamodb-local:latest
    ports:
      - "8051:8000"
